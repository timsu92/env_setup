---
# Profile: Proxmox VM — base setup
# Target: vm inventory group (push model via SSH)
# Run: bin/setup-vm --host <IP>
- name: Provision Proxmox VM base
  hosts: vm
  gather_facts: true
  become: true

  roles:
    - role: apt_nycu_mirror
    - role: apt_cacher_ng
    - role: common_base
    - role: proxmox_guest
    - role: vim
      vars:
        vim_profile: minimal
    - role: git
    - role: zsh
    - role: zplug
    - role: starship
    - role: tmux
    - role: nfs_client
