---
- name: Create Claude Code home directory
  ansible.builtin.file:
    path: "{{ claude_code_dir }}"
    state: directory
    owner: "{{ setup_user }}"
    group: "{{ setup_user }}"
    mode: "0755"
