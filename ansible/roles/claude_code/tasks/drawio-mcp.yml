---
- name: Install drawio-mcp
  ansible.builtin.command:
    cmd: ~/.local/bin/claude mcp add --scope user drawio -- npx -y @drawio/mcp
  become: true
  become_user: "{{ setup_user }}"
  register: claude_code_drawio_mcp_install
  # 如果return code是1且輸出包含"already exists in user config"則代表已經安裝過了，這個任務不應該被視為changed
  changed_when: "'MCP server drawio already exists in user config' != claude_code_drawio_mcp_install.stderr"
  failed_when: "'MCP server drawio already exists in user config' != claude_code_drawio_mcp_install.stderr and claude_code_drawio_mcp_install.rc != 0"
