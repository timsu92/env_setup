---
# Create a directory for self-downloaded plugins
- name: Create self-download Claude plugin directory
  ansible.builtin.file:
    path: "{{ claude_code_plugin_dir }}/self-download"
    state: directory
    owner: "{{ setup_user }}"
    group: "{{ setup_user }}"
    mode: '0755'
