---
- name: Create Claude Code skills directory
  ansible.builtin.file:
    path: "{{ claude_code_skills_dir }}"
    state: directory
    owner: "{{ setup_user }}"
    group: "{{ setup_user }}"
    mode: "0755"
