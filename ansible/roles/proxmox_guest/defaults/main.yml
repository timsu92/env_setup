---
# Groups that are common in Proxmox VMs and LXC (all seven ship via the
# base-passwd package, present on every Debian/Ubuntu target).
proxmox_guest_user_groups:
  - adm
  - sudo
  - dip
  - plugdev
  - dialout
  - audio
  - video
